# Backlog (append-only)

Open items for future sessions. Append at the bottom; never delete or
reorder. When an item is done, check it off and note the session/commit —
don't remove the line.

<!-- TEMPLATE — copy below the last entry:
---
- [ ] **<short title>** (added YYYY-MM-DD by <agent>) — <enough context that
      a fresh agent can act on this without any chat history. Severity if known.>
-->

---
- [ ] **N1: Rewrite architecture doc to match v0.4** (added 2026-07-15 by Super Z) — `docs/InjectX-Architecture.md` is stale vs the v0.4 implementation. Says HAT/DARK/TLS have "no public decryptor" but code has working decryptors for HAT (scheme E1) and TLS (scheme F1); only DARK lacks a decryptor. Says EHI uses "base64 unlock" but code uses 2-stage AES (scheme B1). Lists `python-magic` in requirements but it's neither in `requirements.txt` nor imported. Either rewrite the doc to match v0.4 or mark it explicitly as "historical proposal — see code for actual architecture". Severity: nice to have (doc-only).
- [ ] **N2: Cross-platform setup guide** (added 2026-07-15 by Super Z) — `docs/setup-guide.md` is Windows-only (`C:\Projects\injectx`, `tasklist`, PowerShell examples) but the project works on Linux/Mac too (`frontend/main.js` already handles Linux venv path). Also references "InjectX v0.3.0" while the project is at v0.4.0. Rewrite to be cross-platform or split per-OS; update version refs. Severity: nice to have (doc-only).
- [ ] **N3: Add test infrastructure** (added 2026-07-15 by Super Z) — No `tests/` directory, no `conftest.py`, no `pyproject.toml [tool.pytest]`, no test runner config. The 7 format parsers and 7 decryptors have zero automated coverage — changes to crypto code are verified only by ad-hoc manual testing. Add `pytest` + `pytest-cov` + `ruff` + `mypy` to dev deps; add `tests/` with at least one happy-path test per format (using a known-good sample) and one negative test per format (malformed input). Severity: nice to have but high-leverage — this is the highest-value next session. **PARTIALLY ADVANCED — Session 4 (commit `d2810b1`) shipped `backend/pyproject.toml` with `[tool.pytest.ini_options]`, `[tool.ruff.lint]` (passes clean today), and `[tool.mypy]` (informational, 62 pre-existing errors). Still open: per-format parser/decryptor happy-path tests with sample config files; clean up the 15 F401 unused-import hits so the `F401` ignore can be dropped; tighten ruff rules (add I/UP/B) once the codebase is cleaned up; resolve the 62 mypy errors. N4 (CI) is now the easiest next win — pyproject.toml makes a `pytest && ruff check .` workflow trivial.**
- [ ] **N4: Add GitHub Actions CI** (added 2026-07-15 by Super Z) — No `.github/workflows/`. Pushes to `main` are not validated. Add a minimal workflow running `pytest`, `ruff check`, `npm install` on every push/PR. Depends on N3. Severity: nice to have.
- [ ] **N5: Cap config_store size or persist to SQLite** (added 2026-07-15 by Super Z) — `config_store: dict[str, dict] = {}` in `backend/main.py` grows forever; no eviction, no max size. Each entry includes the full `decrypt_trace` (can be large — 21 attempts × multiple fields for a B1 scheme that fails on all keys). Long-running sessions parsing many configs will accumulate memory. Either (a) cap at N entries with LRU eviction, or (b) persist to SQLite (per architecture doc §10.2 — already planned for v2). Severity: nice to have for power users.
- [ ] **N6: Narrow bare `except Exception:` clauses in parser code** (added 2026-07-15 by Super Z) — 60+ bare `except Exception:` clauses in `backend/decrypt/` and `backend/parser/` swallow errors silently. In decrypt code this is intentional (try-many-keys pattern); leave as-is but add `logger.debug` calls. In parser code, narrow exception types (e.g., `except (KeyError, TypeError, json.JSONDecodeError):`) and log at `WARNING`. Broad refactor; flagged for a future session. Severity: nice to have (code quality).
- [x] **N7: Implement documented env vars** (added 2026-07-15 by Super Z) — Architecture doc §11 documents `INJECTX_PORT`, `INJECTX_HOST`, `INJECTX_UPLOAD_DIR` env vars, but `backend/main.py` reads none of them (port/host hardcoded to `127.0.0.1:8742`, upload dir hardcoded to `~/.injectx/configs`). `NODE_ENV` IS implemented in `frontend/main.js` (for DevTools). Add env var reads with sensible defaults in `main.py`. Severity: nice to have (small change, useful for portable installs / port conflicts). **DONE — commit `8ce5782` (Session 1, post-log) added all three with `test_env_config.py`; verified live Session 2. Follow-up `b08043b` (Session 2) made the CORS loopback origins track `INJECTX_PORT` too.**

---
## Session 2 progress notes (2026-07-15, Claude Fable 5) — append-only

- **N3 partially advanced:** `backend/tests/` now holds `test_env_config.py` (Session 1) + `test_path_validation.py` (Session 2, 8 tests). Still open: `pyproject.toml [tool.pytest]` config, `ruff` + `mypy` setup, `.github/workflows/` CI (N4), and per-format parser/decryptor coverage with sample files. N3 stays open until the parser/decryptor suite exists.
- **Fold into N6 when tackled:** two cosmetic nits in `backend/parser/parse_engine.py` found Session 2 — (a) `_apply_field_map` annotates `dict[str, any]` using the builtin `any` instead of `typing.Any` (~lines 207–208; harmless at runtime, `mypy` would flag it); (b) `_normalize` sets `decryption_status = NOT_ENCRYPTED` for the EHI-unencrypted branch (~line 95) which is then unconditionally overwritten at ~line 112 — dead assignment, final value is still correct. Not worth separate commits; fix during the N6 pass.
- **New hardening recommendation:** `_validate_config_path` has now been the site of two related traversal issues (C1 Session 1 + S2-1 symlink bypass Session 2). A property/fuzz test over path shapes (encoded, symlink chains, `..`, device files) would harden it against a third. See `reviews/2026-07-15-review-2.md` §7.

---
## Session 4 progress notes (2026-07-15, Super Z, cloud) — append-only

- **N3 further advanced:** `backend/pyproject.toml` now shipped (commit `d2810b1`) with `[tool.pytest.ini_options]` (testpaths, --strict-markers), `[tool.ruff.lint]` (select E+F+W, ignore E501 and F401, per-file-ignores for main.py:E402 — passes clean today), `[tool.mypy]` (python_version=3.10, explicit_package_bases, ignore_missing_imports — informational, 62 pre-existing errors). Still open: per-format happy-path tests with sample configs; clean up the 15 F401 unused imports (then drop the F401 ignore); tighten ruff to add I/UP/B; resolve mypy errors so it can become a CI gate.
- **N6 partially advanced:** commits `c593353` (typing.Any + dead assignment in `_normalize`) and `dd0b394` (dead variables in ehi_decrypt/npv_decrypt/hc_parser, duplicate dict key in tls_parser) cleared the F841/F601/B007 rule set. Still open: narrow the 60+ bare `except Exception:` clauses; isort the imports (I001, 26 hits); modernize `Optional[X]` → `X | None` (UP045, 61 hits); tighten the loose substring matches in `_detect_protocol` (`"ws" in k` matches `news`, `passwords`) and `detector._detect_by_content` (`"hc" in name_lower` matches `mhcr.txt`).
- **Fold into N6 when tackled:** the `_ir_to_response` function in `main.py` has a minor cosmetic inconsistency — the `any()` check's exclusion list is missing `payload_parsed` but the loop's `exclude_keys` set includes it. Net effect: if `payload_parsed=True` and everything else is None, `config_dict = {}` (empty but truthy) gets sent instead of None. Not worth a separate commit; fix during the N6 pass.
- **New finding flagged for a local-agent session:** `frontend/main.js:80` sets `sandbox: false` in `webPreferences`. With `contextIsolation: true` and `nodeIntegration: false`, sandboxing COULD be enabled for defense in depth — the preload only uses `require("electron")` which works sandboxed. Not changed this session because (a) I can't test the actual runtime in the cloud sandbox (no display server), and (b) it's a security-sensitive change that benefits from end-to-end testing on a real desktop env. A future local-agent session should set `sandbox: true` and verify the app still loads, file dialogs work, IPC handlers fire, and DevTools open.
- **N4 is now the easiest next win:** with `pyproject.toml` in place, a minimal GitHub Actions workflow can run `pip install -r backend/requirements.txt && pip install pytest ruff && cd backend && pytest && ruff check .` on every push/PR. Particularly valuable given the security-sensitive path-validation code.

---
- [ ] **N8: Add `sandbox` attribute to the notes iframe (defense-in-depth)** (added 2026-07-15 by Claude Fable 5, Session 11) — `renderNotesSandboxed` in `frontend/src/scripts/renderer.js` (~line 861) renders raw HTML from an untrusted config author (HC v2.7 notes) in a srcdoc iframe with **no** `sandbox` attribute. The actual XSS/exfil hole is already closed by the parent CSP in `index.html` (`script-src 'self'` with no `unsafe-inline`, which the srcdoc frame inherits, plus loopback-only `img-src`/`connect-src`) — so this is defense-in-depth, not an open vuln. Add `iframe.setAttribute("sandbox", "allow-same-origin")` (scripts disabled + same-origin kept so the auto-resize `contentDocument` read still works). **Blocked on GUI verification:** Session 8 reported adding a sandbox attr "prevented rendering" (likely a misdiagnosis — several rendering bugs were fixed in that same commit), so a local session must launch the real Electron app, open an HC v2.7 config with notes, and confirm the styled notes still render before shipping. The in-app Chromium browser tool timed out in Session 11, so it wasn't verified there. Severity: Low (hardening). See `reviews/2026-07-15-review-4.md`.

---
## Session 11 progress notes (2026-07-15, Claude Fable 5, local) — append-only

- **N3 substantially advanced:** `backend/tests/test_sample_parsing.py` (commit `653e5bf`) adds the per-format happy-path tests N3 asked for — parametrized over all 32 real samples under `assets/configs/**` (HC×13, EHI×6, ZIV×6, DARK×4, TLS×2). Suite is now 41 tests (was 9). Asserts pipeline returns a `NormalizedConfig`, format matches extension, filename survives normalization. Still open under N3: decryptor happy-path assertions where keys still work (HC A5 + EHI B2 both decode today — worth asserting a known field decrypts), the 15 F401 unused-import cleanup (then drop the ruff `F401` ignore), and the mypy errors. N3 stays open for those.
- **Two known cloud-flagged local-agent tasks still pending, both blocked on the same thing (a working Electron GUI run, which I could not get this session — the in-app browser tool timed out):** (a) N8 above — notes-iframe sandbox attr; (b) the Session 4 note about `frontend/main.js` `sandbox: false` in `webPreferences` — flipping it to `true` for renderer-process sandboxing needs the same end-to-end GUI verification (file dialogs, IPC, DevTools). A future local session with a launchable Electron window should do both together in one verification pass.
- **Fixed this session (not backlog):** `/api/formats` stale data (`308d360`) and a factually-wrong notes-iframe security comment (`289cd39`). See the review report.

---
- [x] **N9: EHI decodes but the UI shows it as empty** (added 2026-07-15 by Claude Fable 5, Session 12) — EHI (HTTP Injector) configs DO decrypt end-to-end (scheme B2, 6/6 samples). The NormalizedConfig gets `proxy_host`, `payload`, `sni`, and `raw_data._all_fields` populated — but **no top-level `host`/`server`** field, because HTTP Injector routes through a proxy, not a direct SSH host. A detail view keyed on `host` therefore looks empty, which is why EHI is perceived as "not working" even though decryption succeeds. **DONE — Session 17 (commit `6d09419`):** fixed on the frontend with a generic "DECODED FIELDS" table that renders every `raw_data._all_fields` entry, hero tiles that fall back to proxy_host/udpserver/sni, and notes that pick up `configMessage`/`file.msg`. An EHI that previously rendered 1 row now shows all 21 fields; no JSON export needed. Verified in-browser. See `reviews/2026-07-16-review.md`.
- [ ] **N10: HA Tunnel (HAT / scheme E1) is implemented but has zero test samples** (added 2026-07-15 by Claude Fable 5, Session 12) — `backend/decrypt/hat_decrypt.py` (AES-128-ECB, keys from `KeyStore.aot`, validates `{"` JSON prefix, handles `profile`/`profilev4`/`configuration` shapes), `backend/parser/hat_parser.py`, `FormatEnum.HAT`, `SchemeEnum.E1`, the router entry, and `.hat`/`.ha` in `ALLOWED_EXTENSIONS` all exist and match HCDecryptor's `aotDecryptor.lib.js`. But `assets/configs/hat/` holds only `.gitkeep` — **HAT has never been verified end-to-end in this project.** To close: obtain a real HA Tunnel Plus `.hat`/`.ha` export, drop it under `assets/configs/hat/`, run `parse_config` on it, and confirm it decodes (or, if the aot keys were rotated like TLS/ZIV, document that). Until then, do not claim HAT "works." Severity: nice to have (format is wired, just unproven).
- [ ] **N12: Route the remaining inline key constants through KeyStore** (added 2026-07-15 by Claude Fable 5, Session 13) — Session 13 wired `INJECTX_KEYFILE` → `get_router()` → `KeyStore._load_keyfile()`, so any KeyStore category (`tls`, `aot`, `evozi`, `ePro`, `slipk`, `sip`, `npv2`, `vhd`) can be updated from an external JSON file with no code change. But three decryptors still hardcode their key material OUTSIDE KeyStore: `decrypt/ziv_decrypt.py` (`_ZIV_PASSWORDS` list), `decrypt/hc_v27_decrypt.py` (v2.7 ChaCha20 constants), and `decrypt/ehi_v2_decrypt.py` (Argon2 constants). Consequence: a rotated **ZIVPN** password can't be dropped in via the keyfile — it needs a source edit. Add a `ziv` category to `KeyStore` (and `_DEFAULT_KEYS`), have `decrypt_ziv` accept/read passwords from the KeyStore instance (router currently calls it without `keys`), and similarly expose the HC-v27/EHI-v2 constants. Then the keyfile is the single supply point for ALL formats. Severity: nice to have (completes the runtime-key story started in N13-adjacent work). Referenced from `docs/key-extraction.md`.
- [ ] **N13: Obtain samples to verify the unproven decryptors (NPV/NSH/VHD/HAT) and identify LNK** (added 2026-07-15 by Claude Fable 5, Session 14) — Five formats have decryptor code but zero sample files, so their algorithms are unverified: HAT (E1, AES-128-ECB — matches HCDecryptor aotDecryptor), NPV (C1, NapsternetV charCode-subtraction cipher), NSH (D1, SocksHTTP AES-128-GCM+PBKDF2), VHD (G1, AES-128-CBC). LNK has NO decryptor at all and is an unknown format — Session 10 noted the user renamed some other config to `.lnk` so Windows wouldn't treat it as a shortcut, so a `.lnk` sample is really some other format to identify by content. Action: drop one real config per format into `assets/configs/<fmt>/`, run `parse_config`, and verify/repair each against real bytes (the DARK approach from Session 14). Until then these show as untested. Severity: nice to have (completes "algorithms for all files"). See `reviews/2026-07-15-review-7.md` for the full algorithm inventory.
- [x] **ZIV (H1) rotated key — DONE Session 15 (commit `f6e91d8`).** Extracted the current password from the ZIVPN Tunnel v2.1.5 APK via androguard static analysis (`o3.a` builds `SecurePart1..SecurePart5`; BouncyCastle PKCS5S2 PBKDF2 1000 iters + AES-GCM). `.ziv` now decodes 6/6. Lesson: "blocked on external key" meant "nobody extracted it yet," not "impossible." See `reviews/2026-07-15-review-8.md`.
- [ ] **N11: TLS (F1) blocked on rotated key — NOT statically extractable (DexProtector); needs dynamic Frida** (updated 2026-07-15 by Claude Fable 5, Session 16) — Session 16 got the TLS Tunnel v8.0.6 APK (com.tlsvpn.tlstunnel) and tried the ZIVPN method. It FAILED because the app ships **DexProtector** (`libdexprotector.so`), a commercial packer that encrypts the app's own strings/classes at runtime. Every AES/GCM reference in the dex was an unprotected ad SDK (Digital Turbine, Conscrypt, Tink, Mintegral); a 148k-string × 5-derivation brute force found no key. The F1 algorithm (AES-256-GCM, IV-first-12, tag-last-16) + the base64-padding fix are correct — only the key is missing, and it's materialized only at runtime. **Path:** dynamic Frida on a rooted device/emulator — hook `SecretKeySpec.<init>`/`Cipher.doFinal` (or native `libtlsvpn.so`), import a `.tls` config, read the key after DexProtector decrypts it. Needs the USER's device (no emulator/root on the dev machine). See `reviews/2026-07-15-review-9.md` + `docs/key-extraction.md`. (added 2026-07-15 by Claude Fable 5, Session 12) — Both algorithms in the codebase are correct and match the authoritative public decryptors (TLS: PANCHO7532/HCDecryptor `tlsDecryptor.lib.js`; ZIV: EstebanZxx/X-Tools `Main.py`). InjectX already carries the only public TLS key and the KMKZ ZIV password (`fubvx788b46v`), and both MAC-fail on the bundled samples because newer app builds rotated the secret. A 256-combo ZIV password/param sweep (Session 12) found nothing. **There is no code fix** — this is gated on external key material (a newer public reverse of TLS Tunnel / ZIVPN, or the user supplying the key/password via the keyfile mechanism in `KeyStore._load_keyfile`). Revisit only when a new key source surfaces. DARK stays proprietary (no public decryptor at all). Severity: blocked-external.

---
- [x] **N14: Implement SNI Host Hunter — Phase 1 (MVP)** — **DONE Session 24 (2026-07-23), commits `9063184` (backend) + `a7ea22e` (tests) + `41a9922` (ui) + `8f88424`/`defc297` (docs), ADRs `e7f612f`.** Shipped `backend/snihunter/` (crt.sh + seedlist discovery, async prober with pure `classify_http`, job store, txt/csv/json export), 7 `/api/sni/*` endpoints + `_validate_seedlist_path` + `INJECTX_ENABLE_SNI_HUNTER` kill switch, bundled Safaricom/Airtel/Telkom-KE seedlists + portal_indicators, `sni` terminal command tree via Electron IPC (ADR-7), 37 tests (suite 54→91, ruff clean), README/architecture/changelog. `httpx` added; `dnspython` deferred to Phase 2. ADR-6/7/8 ratified. Verified end-to-end via curl + a Node harness against the live backend. **Still needs user confirmation in the packaged Electron app** (IPC registration + terminal DOM — this machine can't launch Electron headless). See `reviews/2026-07-23-feature-review.md`. Phase 2 is N15. (added 2026-07-24 by Super Z, Session 23) — Research + design complete in `.context/memory/features/sni-host-hunter.md` (13 sections, ~30k words, includes pseudo-code for `backend/snihunter/` package + IR models + API endpoints + terminal commands + phasing checklist + legal/ethical + ADR-6 proposal + full references). Phase 1 MVP scope: (1) `backend/snihunter/` package with `sources/crtsh.py`, `sources/seedlist.py`, `probe.py`, `store.py`, `export.py`, `models.py`; (2) `data/seedlists/{safaricom-ke,airtel-ke,telkom-ke}.txt` curated from public Techfoe/aimtuto/snihost.com lists; (3) 7 new endpoints under `/api/sni/*`; (4) terminal commands `sni find/scan/jobs/stop/export/seedlists/help`; (5) `httpx` + `dnspython` added to requirements; (6) tests for IR + crt.sh source + seedlist loader + path validation. Phase 2 (sidebar UI module, CertStream, ECH detection, reverse-IP, port checker) and Phase 3 (defensive mode) deferred. **First step for implementing session:** ratify proposed ADR-6 (loopback-bound, ≤200 concurrency, no IP-range scanning) in `plans/decisions.md`, then read §6.5 of the design doc for the full Phase 1 checklist. Open questions in §9 should be answered (or defaults accepted) before coding. Severity: high-value next feature — closes the discover→probe→use loop InjectX's existing config-decrypt pipeline has been missing.

---
- [ ] **N15: SNI Host Hunter — Phase 2 (UI module + CertStream + ECH detection)** (added 2026-07-23 by Claude Code/Opus 4.8, Session 24) — Phase 1 (N14) shipped the terminal-only MVP. Phase 2 per `features/sni-host-hunter.md` §5.2/§6.5: (1) a sidebar "05 · SNI HUNTER" module mirroring the Arsenal dashboard (seedlist picker, scan-config form, live results table with verdict pills + click-to-filter); (2) a "Use as SNI" button on working hosts that patches a config's `sni` field and re-parses; (3) `backend/snihunter/sources/certstream.py` real-time CT watch (`sni watch <domain>`); (4) `backend/snihunter/dns_check.py` **ECH capability detection** — the research refresh found **RFC 9848** (bootstrapping ECH via DNS SVCB/HTTPS records): query the target's HTTPS RR for an `ech=` param to flag ECH-capable hosts (add `dnspython` here — deferred from Phase 1 which used stdlib DNS); (5) reverse-IP lookup (BugScanX feature #4, via public API, NOT active scanning per ADR-6) + open-port checker. Also: tune `backend/snihunter/data/portal_indicators.txt` against real Safaricom/Airtel/Telkom captive portals (feature doc §9 Q3). Severity: high-value follow-on.

---
- [x] **N15: SNI Host Hunter — Phase 2 (UI module + CertStream + ECH detection)** — **DONE Session 26 (2026-07-24), Super Z cloud.** Shipped: 5 new backend modules (`backend/snihunter/dns_check.py` ECH via RFC 9848 DNS HTTPS-RR, `sources/certstream.py` real-time CT watch, `reverseip.py` HackerTarget+PTR, `portcheck.py` 80/443/8080/8443, `apply.py` "use as SNI" with `_original_sni` preservation + audit warning); 5 new `/api/sni/*` endpoints (`/watch`, `/ech`, `/reverseip`, `/portcheck`, `/apply`) all gating on `INJECTX_ENABLE_SNI_HUNTER`; sidebar "05 · SNI HUNTER" module (config panel + live results table + verdict pills + click-to-filter + per-row Use-as-SNI/ECH/PORTS/REV-IP action buttons); 60 new tests (suite 91→151, ruff clean); `dnspython` added to requirements. Verified: pytest 151, ruff clean, live curl of every endpoint (success + error + kill-switch paths, ECH on crypto.cloudflare.com = capable, apply on TelkomNet.ehi preserves `myaccount.telkom.co.ke` as original), Node harness drove real `api.js` `API.sni.*` against live backend, `node --check` all JS. **Needs USER confirmation in the packaged Electron app** (sidebar DOM rendering + IPC handler registration — cloud sandbox can't launch Electron headless). See `reviews/2026-07-24-phase2-review.md`. (added 2026-07-23 by Claude Code/Opus 4.8, Session 24) — Phase 1 (N14) shipped the terminal-only MVP. Phase 2 per `features/sni-host-hunter.md` §5.2/§6.5: (1) a sidebar "05 · SNI HUNTER" module mirroring the Arsenal dashboard (seedlist picker, scan-config form, live results table with verdict pills + click-to-filter); (2) a "Use as SNI" button on working hosts that patches a config's `sni` field and re-parses; (3) `backend/snihunter/sources/certstream.py` real-time CT watch (`sni watch <domain>`); (4) `backend/snihunter/dns_check.py` **ECH capability detection** — the research refresh found **RFC 9848** (bootstrapping ECH via DNS SVCB/HTTPS records): query the target's HTTPS RR for an `ech=` param to flag ECH-capable hosts (add `dnspython` here — deferred from Phase 1 which used stdlib DNS); (5) reverse-IP lookup (BugScanX feature #4, via public API, NOT active scanning per ADR-6) + open-port checker. Also: tune `backend/snihunter/data/portal_indicators.txt` against real Safaricom/Airtel/Telkom captive portals (feature doc §9 Q3). Severity: high-value follow-on.

---
- [x] **N16: SNI Host Hunter — Phase 3 (defensive mode)** — **DONE Session 28 (2026-07-24), commits `0115835` (backend) + `26aaa40` (tests) + `6c19b9d` (ui) + `a570817`/`c0124e8` (docs), ADR `f79c263`.** Shipped `backend/snihunter/defensive.py` (`probe_fronting(sni,host)`: raw HTTP-over-TLS mismatched-Host probe + TLS-cert fingerprint comparison + pure `classify_fronting` verdict), `SniFrontingResult`/`FrontingVerdict` models, `POST /api/sni/fronting`, `sni fronting <sni> <host>` terminal command (IPC per ADR-7), 15 tests (suite 151→166). ADR-9 ratified (single-target, read-only, non-exploitative). Verified live (curl + Node harness). Terminal + API only — sidebar UI is N17. Also fixed pulled test-infra breakage (`710890d`: pytest-asyncio). See `reviews/2026-07-24-phase3-review.md`. (added 2026-07-24 by Super Z, Session 26) — Phase 2 (N15) shipped the discover→probe→use loop, the sidebar UI, CertStream watch, ECH detection, reverse-IP, port checks, and "use as SNI". Phase 3 per `features/sni-host-hunter.md` §5.2 deferred items + §3.5 spoofing detection: (1) **verify an ISP's zero-rating enforcement** — given an ISP + a hostname, probe whether the ISP correctly blocks SNI spoofing (the same probing machinery runs in reverse for defensive purposes); (2) **nDPI-style SNI/Host-header mismatch detection** — flag domain-fronting attempts where the TLS SNI doesn't match the HTTP `Host` header (reference: ntop/nDPI issue #2573); (3) **per-host TLS fingerprint comparison** — does the cert chain change when SNI changes? A signal for domain-fronting detection. Lower priority than N3/N4 (test infrastructure + CI) — Phase 3 is a research-grade feature, not a user-facing one. Severity: nice-to-have (advances the defensive half of the dual-use framing).

---
- [ ] **N17: SNI Host Hunter — expose the defensive fronting probe in the sidebar UI** (added 2026-07-24 by Claude Code/Opus 4.8, Session 28) — Phase 3 (N16) shipped the defensive `probe_fronting` as `POST /api/sni/fronting` + a `sni fronting <sni> <host>` terminal command, but there is no sidebar UI for it (Phase 3 is terminal + API only). Add a defensive panel to the "05 · SNI HUNTER" module (or a per-result-row "FRONT?" action that runs the probe against the selected config's current SNI as `host`), rendering the verdict pill (enforced/bypassable/indeterminate/error) + the cert-fingerprint and DNS-consistency signals. The IPC/api plumbing already exists (`API.sni.fronting(sni, host)` → `sni-fronting` handler). Keep ADR-9 framing (single-target, defensive, "verify enforcement" not "bypass"). Severity: nice-to-have (completes the UI surface; the capability is already usable from the terminal).

---
- [x] **N17: SNI Host Hunter — expose the defensive fronting probe in the sidebar UI** — **DONE Session 29 (2026-07-24), Super Z cloud.** Shipped a "DEFENSIVE PROBE — FRONTING" panel at the bottom of the 05 · SNI Hunter module (below the scan-config/results two-column layout). Two inputs (SNI + Host) + PROBE button → color-coded verdict banner (green=enforced, red=bypassable, amber=indeterminate, muted=error) + 2-column detail grid (HTTP status, server header, TLS handshake, both cert CNs, cert-changes-with-SNI, host-covered-by-cert, DNS consistency, target IP, elapsed) + the backend's plain-language notes. Frontend-only — the IPC/preload/api plumbing (`sni-fronting` handler, `sniFronting` bridge, `API.sni.fronting`) was already there from Session 28. Verified via Node harness driving real `api.js` `API.sni.fronting()` against live backend (cloudflare-vs-example mismatch, wikipedia same-tenant, empty-SNI error path). `node --check` all JS, backend 166 tests unchanged, ruff clean. **Needs USER confirmation in the packaged Electron app** (DOM rendering + button wiring — cloud sandbox can't launch Electron headless). **SNI Host Hunter is now feature-complete across all 3 phases + the full UI surface.** See `reviews/2026-07-24-n17-review.md`. (added 2026-07-24 by Claude Code/Opus 4.8, Session 28) — Phase 3 (N16) shipped the defensive `probe_fronting` as `POST /api/sni/fronting` + a `sni fronting <sni> <host>` terminal command, but there is no sidebar UI for it (Phase 3 is terminal + API only). Add a defensive panel to the "05 · SNI HUNTER" module (or a per-result-row "FRONT?" action that runs the probe against the selected config's current SNI as `host`), rendering the verdict pill (enforced/bypassable/indeterminate/error) + the cert-fingerprint and DNS-consistency signals. The IPC/api plumbing already exists (`API.sni.fronting(sni, host)` → `sni-fronting` handler). Keep ADR-9 framing (single-target, defensive, "verify enforcement" not "bypass"). Severity: nice-to-have (completes the UI surface; the capability is already usable from the terminal).
