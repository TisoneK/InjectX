# InjectX Review — 2026-07-16 (Session 20)

**Agent:** Claude Code (claude-fable-5), local macOS · **Role:** engineer
**Task (user):** (1) Logs page missing a copy button; (2) Arsenal page
"cramped and non-functional"; (3) big feature — a realistic terminal
command system (e.g. `targets help`, `targets purge <id|file|all>`,
`targets debug <...>`).

## What shipped (`feat(ui)` `f99fe9b`)

**Terminal command system.** Replaced the flat `CONSOLE_COMMANDS` lookup
with a namespaced parser/dispatcher (`runCommand` + a `CMD` tree). It's
shared by the Terminal view and the activity-log input through pluggable
IO surfaces (`terminalIO()` / `consoleIO()`), so both get the same
commands; the terminal also gets aligned-table output (`fmtTable` +
`white-space: pre-wrap`). Commands:
- `help · clear · version · about`
- `targets [list] | info | open | debug | export | purge [id|file|all] |
  import <filepath> | count | help`
- `logs [list [n]] | clear | copy`
- `system [status] | formats | health`
- `assets import`
Targets resolve by short/full id or filename substring with ambiguity
detection (`pickTarget`). `targets debug` shows scheme, file location and
a capped (20-row) decrypt-trace table so a 200-key brute-force doesn't
flood. `status`/`purge` kept as aliases of `system status` /
`targets purge`.

**Arsenal dashboard.** The old static text wall became a live capability
dashboard: a summary strip (formats / operational / targets loaded /
decoded), accurate per-format status pills (OPERATIONAL / PARTIAL /
NEEDS KEY / UNTESTED / NO DECRYPTOR / PLAIN — matching the real decryptor
state), scheme id, and live `LOADED · DECODED` counts computed from the
loaded targets. Cards with loaded targets are clickable → open the
Targets view filtered to that format (new `state.formatFilter`, cleared
when a status pill is clicked).

**Logs copy.** Added a `COPY LOG` button (copies the whole archive) and a
hover copy button per entry. Extracted a shared `copyText()` helper (also
used by `copyBtn` and the terminal `logs copy`).

## Verification
In-browser against 31 real targets (injected the config list + a mocked
`API.getConfig`, since the http preview is CORS-blocked from the backend):
- `targets` prints an aligned 31-row table; `targets count` shows the
  format breakdown; `targets debug <id>` renders the trace table; `hc`
  correctly reports "matches 13 targets"; unknown commands error cleanly.
- Arsenal shows 11 formats / 3 operational / 31 loaded / 29 decoded with
  correct per-format counts; clicking HTTP Custom filters Targets to its
  13 configs.
- Logs page has COPY LOG + CLEAR LOG + per-entry copy.
- No console errors. `node --check` clean.

## Open items
- N10 (HAT sample), N11 (TLS key via Frida), N12 (route inline keys
  through KeyStore), N13 (NPV/NSH/VHD/LNK samples) unchanged.
- Terminal command ideas for later: `targets export all`, command
  history (up-arrow), tab-completion, `keyfile add <fmt> <key>` to feed
  the INJECTX_KEYFILE mechanism live.
